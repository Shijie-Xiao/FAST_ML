__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
.vscode/
.idea/

# Large inputs fetched by scripts/download_data.py, not tracked in git.
# Listed without a trailing slash so a local symlink is ignored too.
data/training_data
data/ensemble/chi_s_*.nc
data/ensemble/*/chi_s_*.nc
data/fastml_training_data_*.tar
*.tar
*.tar.gz

# Superseded normalisation statistics; see scripts/compute_spatial_stats.py.
data/spatial_stats_2003_2021.pkl

# Generated outputs (NetCDF, metrics, figures). Only the images referenced by
# README.md are copied into img/ and tracked; everything else is local-only.
results/


# ── training/ pipeline (JAMES reviewer release) ──────────────────────────────
# Large inputs and outputs of the training pipeline are local-only.
training/data/
training/training_data/
training/ensemble_tracks/
training/ensemble_runs/
training/ensemble_data/
training/runs/
training/wandb/
training/track_model/processed*/
training/track_model/__pycache__/
training/fast_ode/__pycache__/
training/_gefs_nc/
training/*.log
training/ckpts/
training/vortex_inversion/__pycache__/

# Full-run slurm log (verification runs)
training/full_run_*.log
